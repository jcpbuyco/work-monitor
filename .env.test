# Loaded by `bun test` before any test code runs. Pins the timezone for both
# JS Date and bun:sqlite's localtime (the test runner otherwise forces JS to UTC
# while SQLite keeps the system zone), however the tests are invoked.
TZ=Europe/Berlin
